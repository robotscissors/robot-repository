<div class="row">
  <div class="col-md-5 jumbotron">

      <h2><strong>
        <%= @document.title %>
      </strong></h2>
      <p><%= @document.description %></p>
      <p><strong>Keywords:</strong><br />        <small><%= @document.keywords %></small></p>
      <br /><br />
      <small><%= "Original: #{@document.created_at.localtime}"%><br /><%="Modified: #{@document.updated_at.localtime}" %></small>

      <div class="pull-right margin-before-50">
           <%= link_to "Edit Document", edit_document_path, class: 'btn btn-success' %>
           <%= if current_user.admin?
             link_to "Delete", @document, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this post?' }
           end %>
      </div>


  </div>
  <div class="col-md-offset-1 col-md-5 well well-lg">
    This a preview of the document if possible.
  </div>

</div>
